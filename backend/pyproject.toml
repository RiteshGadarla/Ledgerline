[project]
name = "ledgerline-api"
version = "0.1.0"
description = "Ledgerline reconciliation engine and API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "redis>=5.0",
    "google-genai>=0.3",
    "polars>=1.9",
    "fastexcel>=0.12",
    "pdfplumber>=0.11",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "alembic>=1.13",
    "argon2-cffi>=23.1",
]

[dependency-groups]
dev = [
    "ruff>=0.8",
    "mypy>=1.13",
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "hypothesis>=6.115",
    "import-linter>=2.1",
    "httpx>=0.27",
    "fpdf2>=2.8",
    "openpyxl>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app", "contracts", "money", "engine", "datagen", "scripts", "llm", "ingest", "db"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = ["migrations/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Body"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]

[tool.mypy]
python_version = "3.12"
strict = true
disallow_any_explicit = false
packages = ["app", "contracts", "money", "engine", "datagen", "scripts", "llm", "ingest", "db"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

[tool.importlinter]
root_packages = ["app", "contracts", "money", "engine", "datagen", "llm", "ingest", "db"]
include_external_packages = true

[[tool.importlinter.contracts]]
name = "engine stays pure"
type = "forbidden"
source_modules = ["engine"]
forbidden_modules = ["db", "llm", "app", "random"]

[[tool.importlinter.contracts]]
name = "datagen stays pure"
type = "forbidden"
source_modules = ["datagen"]
forbidden_modules = ["db", "llm", "app"]

[[tool.importlinter.contracts]]
name = "routers never touch SQLAlchemy models directly"
type = "forbidden"
source_modules = ["app.routers"]
forbidden_modules = ["db.models"]
allow_indirect_imports = "true"
