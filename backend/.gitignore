.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# Local environment and secrets. The example is the one env file that is
# meant to be committed, so it has to be negated after the .env.* sweep.
.env
.env.*
!.env.example
